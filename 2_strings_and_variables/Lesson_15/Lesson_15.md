```rootsql
15.1 Ввод
# Давайте предположим, что нам надо получить от пользовотеля его возраст.
Мы знаем, что функция input() возвращает строку.
Чтобы конвертировать ее в число, мы можем использовать функцию int()
# Подобным образом, чтобы конвертировать число в строку, можно использовать функцию str(). Она может пригодится когда, 
например, нам нужно число при конкатенации.
# Для конветирования в float, соответственно используется функция float().
```
```rootsql
15.2 Ввод
# Вы можете использовать функцию input() несколько раз, чтобы получить соответсвенное количество данных от пользователя
# Когда исполняется функция input(), работа программа останавливается, пока не получит данные от пользователя
```
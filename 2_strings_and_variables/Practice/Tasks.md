```rootsql
Задача 9.2 'Найди баг!':

Исправьте полученный код на Python для получения необходимого вывода:
print('I'm learning Python!')
print("A number enclosed in double quotes: "42"")

Подсказка:

Строки помещаются в двойные или одинарные кавычки. 
Для добавления двойных кавычек в строки с двойными кавычками 
или одинарных кавычек в строки с одинарными кавычками вам нужно экранировать их.
Помните, что вам нужно экранировать кавычки в строке, чтобы исправить ошибки в коде.
Для этого используйте \ перед кавычками, которые вам надо экранировать.
```
```rootsql
Задача 10.2 'Больше строк':

print('A B C D')
Полученный код выводит A B C D (каждая буква отделяется пробелом).
Измените код, чтобы вывести каждую букву отдельной строкой, получив следующий вывод:
A
B
C
D

Подсказка:

Изучите урок Python Strings, если вы не помните, что такое перевод строки.
```
```rootsql
Задача 11.2 'Строковые операции':

Полученный код выводит строку "ni".
print("ni")
Измените код, чтобы вывести строку, повторяемую 3 раза, с добавлением ! (восклицательный знак) в конце вывода.

Подсказка:

Помните, что вы можете соединять строки при помощи оператора +.
```
```rootsql
Задача 13.2 'Развлекаемся с показателями степени':

x = 7
print(x)
Предоставленный код содержит значение 7 в переменной и выводит его.
Измените код таким образом, чтобы в выводе получить переменную, увеличенную в 3 раза.

Подсказка:

Возведение в степень можно использовать для многократного умножения числа на себя.
```
```rootsql
Задача 15.1 'Произведение целых чисел':

print(input()*input())
Кто-то написал код, который берет два целых числа в качестве ввода и выводит их результат.
Однако код приводит к ошибке. 
Исправьте код, чтобы вывести результат двух целых вводов
Пример ввода:
3
6
Пример вывода:
18

Подсказка:

Помните, что функция input() дает строку, даже если ввод содержит число.
```
```rootsql
Задача 15.2 'Несколько переменных':

Используя код ниже, выведите ввод x, повторяемый y раз.
Пример ввода:
awesome
3
Пример вывода:
awesomeawesomeawesome

Подсказка:

Вы можете использовать несколько переменных для добавления нескольких вводов в вашу программу. 
Например, в следующем коде делается два ввода, которые хранятся в двух переменных x и y.
x = input()
y = input()
Помните, что функция input() дает строку. Вам необходимо преобразовать результат в соответствующий тип, 
если вам необходимо получить число.
```
```rootsql
Задача 18 'Проект по кодингу (Простой калькулятор)':

Напишите программу, которая берет два целых числа в качестве ввода и выводит их сумму. 
Пример ввода:
2 
8
Пример вывода: 
10

Подсказка:

Помните, что input() дает строку.
```
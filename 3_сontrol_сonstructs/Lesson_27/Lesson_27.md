```rootsql
27.1 Циклы while 
# Циклы while используются для повторения фрагмента кода. 
В качестве примера можно представить программу, которая запрашивает ввод от пользователя, 
и каждый раз, когда он это делает, программа исполняет один и тот же фрагмент кода.
# Код в теле цикла выполняется многократно. Это называется итерацией.
# Ниже приведен цикл while с переменной, который запрограммирован посчитать от 1 до 5; после этого цикл останавливается.
i = 1
while i <= 5:
    print(i)
i = i + 1
print("Finished!")
При каждом повторении значение переменной i возрастает на 1, пока не достигнет значения 5. 
Таким образом, функция print будет вызвана 5 раз.
```
```rootsql
27.2 Циклы while 
# Вы можете использовать многочисленные выражения в цикле while. 

Например вы можете использовать условный оператор if, для принятия решений. 
В качестве примера представим игру, где нам нужно пройтись по всем действиям игроков, 
и в зависимости от действия дать или снять соответственные очки.
x = 1
while x < 10:
    if x % 2 == 0:
        print(str(x) + " is even")
    else:
        print(str(x) + " is odd")
x += 1 
Данный код использует операторы if/else внутри цикла while чтобы определить четные и нечетные цифры в промежутке от 1 до 10.
# str(x) конвертирует число x в строку, чтобы потом оно могло быть использовано в конкатенации.
# В консоле вы можете остановить выполнение программы с помощью комбинации Ctrl+C или просто закрыть программу.
```
```rootsql
27.3 Циклы while 
# 
```
```rootsql
27.4 Циклы while 
# 
```
```rootsql
29.1 Функция range 
# Функция range возвращает последовательность чисел.
По умолчанию, оно начинается с 0, возрастает на 1 и останавливается до указанной цифры.
Данный код возвращает список из 10 последовательных целых чисел с приращением 1 начиная со значения 0.
numbers = list(range(10))
print(numbers) 
# Чтобы вывести range как список, нам надо явно конвертировать его в список, используя функцию list().
```
```rootsql
29.2 Функция range 
# Если функции range присвоить один аргумент, объект будет иметь диапазон от 0 до указанного аргумента. 
Если два аргумента, то будет создан объект с диапазоном от первого аргумента до второго.
# Запомните, второй аргумент не входит в поочередность, так что range(3,8) не будет включать в себя число 8.
```
```rootsql
29.3 Функция range 
# Функции range может присваиваться третий аргумент, который задает интервал числовой последовательности. 
Третий аргумент так же называется шагом (step).
# Мы так же можем создать список с нисходящей поочеродностью. 
Для этого в качестве третьего аргумента надо использовать негативное число, например list(range(20, 5, -2)).
```
```rootsql
29.4 Циклы for
# Цикл for часто используется, когда нужно выполнить какой-то код определенное количество раз. 
Для этого циклы for и объекты комбинируются.
# Не нужно вызывать функцию list для объекта range, когда он используется в цикле for, 
потому что он не индексируется и список не нужно создавать.
```